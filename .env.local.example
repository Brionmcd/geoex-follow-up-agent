# GeoEx Follow-Up Agent - Environment Variables
# Copy this file to .env.local and fill in the values

# ==============================================
# Anthropic API (Required)
# ==============================================
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ==============================================
# Sugati/Salesforce Configuration (Optional)
# ==============================================
# Set to 'true' to use real Salesforce API
# Set to 'false' or leave unset to use sample data
USE_LIVE_SALESFORCE=false

# Salesforce Instance URL
# Get this from your GeoEx Salesforce admin
# Format: https://[instance].my.salesforce.com or https://[instance].sandbox.my.salesforce.com
SALESFORCE_INSTANCE_URL=https://geoex--sandbox.sandbox.my.salesforce.com

# OAuth Connected App Credentials
# These need to be created in Salesforce Setup > App Manager > New Connected App
# Requires: API access, OAuth scopes for data access
SALESFORCE_CLIENT_ID=your_salesforce_client_id_here
SALESFORCE_CLIENT_SECRET=your_salesforce_client_secret_here

# Set to 'false' for production, 'true' for sandbox (default: true)
SALESFORCE_IS_SANDBOX=true

# ==============================================
# Notes for GeoEx IT Team
# ==============================================
# To enable live Salesforce integration:
#
# 1. Create a Connected App in Salesforce:
#    - Setup > App Manager > New Connected App
#    - Enable OAuth Settings
#    - Select scopes: "Access and manage your data (api)", "Perform requests on your behalf at any time (refresh_token, offline_access)"
#    - Set callback URL (can be placeholder for server-to-server)
#
# 2. Generate Client Credentials:
#    - After creating the app, click "Manage Consumer Details"
#    - Copy the Consumer Key (Client ID) and Consumer Secret (Client Secret)
#
# 3. Ensure the API user has:
#    - Read access to Contact, Trip__c, Booking__c objects
#    - Create access to Task object (for logging follow-ups)
#    - Field-level security for all custom fields used
#
# 4. Custom field requirements (verify these exist in your org):
#    - Contact: Passport_Number__c, Medical_Form_Status__c, Waiver_Status__c,
#              Emergency_Contact__c, VIP__c, Previous_Trips__c, Follow_Up_Count__c,
#              Last_Contact_Date__c
#    - Trip__c: Departure_Date__c, Destination__c, Total_Travelers__c
